import { GetBadgeIconPipe } from './get-badge-icon-pipe';

describe('GetBadgeIconPipe', () => {
  it('create an instance', () => {
    const pipe = new GetBadgeIconPipe();
    expect(pipe).toBeTruthy();
  });
});
