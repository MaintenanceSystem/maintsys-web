import { GetBadgeStatusPipe } from './get-badge-status-pipe';

describe('GetBadgeStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new GetBadgeStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
