import { GetDotStatusPipe } from './get-dot-status-pipe';

describe('GetDotStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDotStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
